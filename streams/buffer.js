// e uma representação no espaço na memoria ded um computador usado para tyransitar dados de uma maneira muito rapido

// bufer que uma forma nativa  que o node criou para gente conseguir ler e escrever dados na memoria do computador 

// entendeno melhor 
 //  criando um buffer 
 const buf = Buffer.from("hello")

 console.log(buf.toJSON())
 // buffer e a representação de dados na memoria que o node utilizar  